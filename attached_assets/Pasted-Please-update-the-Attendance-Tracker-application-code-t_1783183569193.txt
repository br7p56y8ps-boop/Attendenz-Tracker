Please update the Attendance Tracker application code to implement the following feature changes, optimizations, and calculations across the Home and Subjects states:

### 1. Home Tab: Interactive Button State (Undo/Redo Selection)
- Modify the selection behavior for the action buttons ("Attended", "Missed", "Off") on the Home screen cards.
- **Undo Capability:** If a user taps an already active/selected button, it should toggle OFF (resetting back to an unselected state).
- **Instant Recalculation:** Ensure that selecting, changing, or deselecting any button instantly updates the state, recalculating the total class numbers and the overall attendance percentage for that subject in real-time.

---

### 2. Home Tab: Dynamic Bunking Alert Messages & Color Customization
Update the subtext notification message under each class card based on a target threshold of 75% attendance. Use the following dynamic text and conditional styling rules:

- **Case 1 (Safe to Miss):** If the user can miss upcoming classes while keeping their total attendance percentage at or above 75%:
  * **Text format:** `✌️ You can bunk 'x' class` (or `classes` if x > 1)
  * **Styling:** The variable `'x'` must be styled in **Green color**.

- **Case 2 (Cannot Miss):** If the user cannot miss the current/next classes without dropping below 75%, or if they are already below 75%:
  * **Text format:** `🥺 You can't miss it; Need to attend 'x' classes`
  * **Styling:** The variable `'x'` must be styled in **Red color**.

*(Note: Ensure the 'x' variable is wrapped in its own inline UI element like a `<span>` or Text component to target its color uniquely without affecting the rest of the text sentence).*

---

### 3. Ward Postings & Subject Logic: Calculation Adjustments
Update the data schema and mathematical logic governing how class tracking behaves, specifically taking note of Ward postings (e.g., Pediatric Surgery Wards):

- **Double Planned Classes:** Double the initial value for the "Total Planned" number of classes across the subjects/ward postings (e.g., if it was 24, change it to 48).
- **Robust Calculation Formula:** Because some ward schedules vary and might not conduct all allotted planned classes, ensure all current attendance percentage logic calculations rely strictly on actual logged data:
  $$\text{Attendance Percentage} = \left( \frac{\text{Classes Attended}}{\text{Classes Attended} + \text{Classes Missed}} \right) \times 100$$
- **Safety Check:** Prevent a `NaN` or division-by-zero error if a subject has 0 attended and 0 missed classes by defaulting the percentage display to `100%` or `--` until a button is selected.
- Update the remaining class thresholds dynamically using the newly doubled planned totals minus the current sum of attended and missed classes.

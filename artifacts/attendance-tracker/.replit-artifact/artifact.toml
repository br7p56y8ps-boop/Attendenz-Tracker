kind = "web"
previewPath = "/"
title = "Attendance Tracker"
version = "1.0.0"
id = "artifacts/attendance-tracker"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22620

[services.development]
run = "pnpm --filter @workspace/attendance-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/attendance-tracker", "run", "build" ]
serve = "static"
publicDir = "artifacts/attendance-tracker/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22620"
BASE_PATH = "/"
